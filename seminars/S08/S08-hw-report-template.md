# HW08 – PyTorch MLP: эксперименты и выводы

## 1. Кратко: что сделано

- Какой датасет выбран (A/B/C) и почему.
- Что сравнивалось (E0-E4) и какая цель экспериментов.

## 2. Среда и воспроизводимость

- Python:
- torch / torchvision:
- Устройство (CPU/GPU):
- Seed:
- Как запустить: открыть `HW08.ipynb` и выполнить Run All.

## 3. Данные

- Датасет: (KMNIST / EMNIST Balanced / CIFAR10)
- Разделение: train/val/test (например, train split 80/20 + test из torchvision)
- Трансформации (transform): (ToTensor / Normalize / другое)
- Комментарий (1-3 предложения): что важно в данных (число классов, размерность, сложность)

## 4. Базовая модель и обучение

- Модель MLP (кратко): число скрытых слоёв, размеры слоёв, активация
- Loss: CrossEntropyLoss
- Optimizer: (Adam, lr=...)
- Batch size:
- Epochs (макс):
- EarlyStopping: (patience=..., metric=val_accuracy или val_loss)

## 5. Эксперименты

Опишите, что меняли. Формулировки должны быть короткими и сопоставимыми.

- E0 (baseline): 1 скрытый слой, без Dropout/BatchNorm
- E1 (глубже/шире): 2 скрытых слоя и/или больше нейронов, без Dropout/BatchNorm
- E2 (Dropout): как E1 и Dropout(p=...)
- E3 (BatchNorm): как E1 и BatchNorm
- E4 (EarlyStopping): лучший из (E2/E3) и EarlyStopping

## 6. Результаты

Здесь не нужно переписывать все числа в текст – достаточно ссылки на артефакты и короткого вывода.

- Таблица результатов: `./artifacts/runs.csv`
- Лучшая модель: `./artifacts/best_model.pt`
- Конфиг лучшей модели: `./artifacts/best_config.json`
- Кривые лучшего прогона: `./artifacts/figures/curves_best.png`

Короткая сводка (3-6 строк):

- Лучший эксперимент: (E2 или E3 или E4)
- Лучшая val_accuracy:
- Итоговая test_accuracy (для выбранной лучшей модели):
- Наблюдения по переобучению: (есть/нет, по кривым)

## 7. Анализ

(6-12 предложений)

- Где на графиках видно переобучение (если видно).
- Как Dropout повлиял на train/val (что стало с разрывом).
- Как BatchNorm повлиял на сходимость/стабильность.
- Что сделал EarlyStopping (на какой эпохе остановил, помог ли).
- Почему выбранный лучший конфиг разумен именно для этого датасета.

## 8. Итоговый вывод

(3-6 предложений)

- Какой конфиг вы бы взяли как базовый и почему.
- Что бы попробовали улучшить дальше (1-2 пункта, без реализации).

## 9. Приложение (опционально)

Если вы делали дополнительные сравнения (не обязательно):

- Adam и SGD с momentum
- другая активация / инициализация
- дополнительные графики: `./artifacts/figures/...`
